1. ✅ 已修复：修改整单应付金额，产生整单改价优惠金额后，商品行上的折扣、折后价已置灰禁止点击
   - 新增 `updateWholeOrderInputsDisabled(disabled)` 函数，统一控制输入框禁用/启用
   - 整单改价确认后（`applyWholeOrderChangeFromPopover`）自动禁用所有商品行折扣/折后价输入框
   - 移除整单优惠（`removeWholeOrderChange`）时自动恢复输入框可用状态
   - 输入框禁用样式：灰色背景(#f3f4f6)、灰色文字(#9ca3af)、禁止点击(pointer-events: none)

2. ✅ 已修复：修改整单应付金额，产生整单改价优惠金额后，商品的折扣、折后价、剩余应付金额已更新显示
   - `applyWholeOrderChangeFromPopover` 中已正确实现按剩余应付比例分摊整单改价优惠
   - 分摊后自动更新 `[data-field="discount"]`（折扣）、`[data-field="finalPrice"]`（折后价）输入框
   - 通过 `ensureActionRowPayable(actionRow)` 更新操作行应付金额显示
   - `normalizeAndRenderMoneyCards()` 函数已正确跳过标记了 `wholeOrderModified='1'` 的商品行（规则6.8），避免分摊结果被覆盖
