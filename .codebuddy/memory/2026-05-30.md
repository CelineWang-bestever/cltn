# 2026-05-30

## 开单收银需求分析
- 读取 `开单收银模块优化需求清单.xlsx`（49行数据，1个Sheet"需求池管理"）
- 去重后共45条有效需求，按优先级分组分析
- 生成输出文件到 `开单收银需求清单/` 目录：
  - `开单收银需求优先级分析.html` — 交互式HTML报告（按优先级分组、可筛选状态）
  - `开单收银需求优先级分析.xlsx` — Excel版（含3个Sheet：原始顺序/优先级排序/统计）
- P0:16项（核心流程阻塞），P1:13项（高效体验），P3:14项（体验优化）
- 第1梯队10项（PRD+设计就绪可立即开发），第2梯队13项，第3梯队12项
- 关键发现：3处重复需求、2项跨部门依赖（转交鑫）、4项待补充信息

## 第二轮调整（去重/去转交鑫/去已验收）
- 移除全部"转交鑫"和"已验收"需求（共8项）
- 权益置换去重统一为 P1，撤销订单权限归到 P3
- 当前 HTML 报告：总计36项（P0:13, P1:9, P3:14）
- Excel 暂不维护，最后统一修改

## 第三轮去重
- "开定制权益卡优化编辑能力" 与 "PAD新增定制权益开单" 为同一需求，合并至 P0
- 当前总计35项（P0:13, P1:9, P3:13）

## 第四轮调整
- 移除"搜索商品增加重置按钮" → 总计34项（P0:13, P1:9, P3:12）
- "赠送权益消耗后退款退次数退原卡"（P0 #5）状态更新：设计已完成，已进入开发

## 当前终态
- 总计34项（P0:13, P1:9, P3:12）
- PRD完成:10, 设计完成:12, 开发中:1, 未启动:11
- 已排除14项

## PAD端需求功能清单
- 从 create-order-pad.html（18620行）和 customer-detail-pad.html（4182行）源码中提取功能点
- 生成 `开单收银需求清单/PAD端需求功能清单.html`
- 开单收银页：102项功能（14个模块）| 客户详情页：27项功能（12个模块，含78个子功能点）
- 关键发现：客户详情页7个Tab仅1个已实现，置换/升卡/充卡在详情页显示"开发中"
- 总计 129+ 功能点

## 第五轮调整 — 补充涉及端信息
补充7项需求的平台标签：
- PAD/PC端：客户权益卡项增加备注外露、权益卡详情展示顺序调整、通卡增加搜索功能
- APP/Pad/PC全端：撤销订单加入功能权限、预约误触已到店后增加撤回、PC/APP居家产品显示产品编码（新增Pad）、机器人/非机器人项目合并下单（新增APP）

## 第六轮调整 — 继续补充涉及端
9项需求补全平台标签：
- APP/Pad/PC全端：PC单次消耗数量（新增APP/Pad）、会员标签允许为空、三端核销一致、三端物品寄存一致、台账赠金/本金区分、台账统计优化（新增APP）、订单修改日期备注、导入卡项退卡
- APP端：手机端默认头像改为女性

## 当前终态（全端覆盖）
- 总计34项需求均已标注涉及端，无空白项
- APP/Pad/PC 全端：20项 | Pad/PC：8项 | APP：2项 | PC：1项 | 未明确：3项（台账统计仅Pad/PC→已改全端 等）

## 第七轮调整 — 对照PAD端设计成果更新需求状态
对照 PAD端需求功能清单.html（从create-order-pad.html + customer-detail-pad.html 提取的129个功能点），确认以下需求在PAD/PC端代码中已有设计：

**新标记为"设计完成"（4项）：**
- P0-9 PC单次消耗数量：代码已有单次护理区域+数量步进器(#15, #23) → PRD完成 + 设计完成
- P0-10 会员标签允许为空：customer-detail已有标签删除功能(§9.3) → PRD完成 + 设计完成
- P3-7 PC/APP居家产品显示产品编码：data-status已是设计完成，修复badge（之前错标"未启动"）
- P3-10 通卡增加搜索功能：卡项搜索(#50)+疗程卡搜索(#51)已覆盖通卡

**确认已有设计（状态不变，5项）：**
- P1-4 赠金/本金区分 → 已有分离展示
- P3-2 续充赠送商品/赠金 → 已有充值+赠送权益管理
- P3-6 PAD挂单提示 → 已有挂单/取单机制
- P0-4 定制权益开单 → 已有自定义疗程卡
- P0-5 赠送权益退款 → 开发中

**当前统计更新：**
- 设计已完成：12 → 15项
- 未启动：13 → 12项

## 第八轮调整 — 统一状态模型为单一状态推进
用户确认正确状态推进顺序：**未启动 → 设计中 → 设计完成 → PRD完成 → 开发中 → 验收中 → 已上线**（每个需求只保留一个状态，取最靠后的）

**修改内容：**
- CSS新增 `badge-designing`（紫色）、`badge-accept`（橙色），复用 `badge-done`（绿色）
- 去掉所有叠加badge，改为按最新阶段显示单一状态：
  - P0-4 定制权益开单：PRD完成 + 设计完成 → **PRD完成**
  - P0-5 赠送权益退款：PRD完成 + 设计完成 + 开发中 → **开发中**
  - P0-9 PC单次消耗：PRD完成 + 设计完成 → **PRD完成**（data-status更新）
  - P0-10 会员标签为空：PRD完成 + 设计完成 → **PRD完成**（data-status更新）
- 统计卡片扩充为11个（4个优先级 + 7个状态）
- 筛选栏和概要文本同步更新

**当前统计（单一状态）：**
- 未启动:11 | 设计中:0 | 设计完成:13 | PRD完成:9 | 开发中:1 | 验收中:0 | 已上线:0

## create-order-pad.html 精简拆分重构
将 18,620 行的单文件拆分为三层架构：

**文件拆分结果：**
| 文件 | 大小 | 说明 |
|------|------|------|
| `create-order-pad.html` | 1,055行 / 57KB | 纯 HTML 结构（引用外部CSS/JS） |
| `create-order-pad.css` | 137KB | 独立样式文件（7160行CSS） |
| `create-order-pad.js` | 442KB | 独立脚本文件（10430行JS） |
| `components/` | 17个文件 | 独立弹窗/组件参考模板 |

**拆分方法：**
- CSS 从 `<style>` 标签提取到 `.css` 文件，HTML 中用 `<link>` 引用
- JS 从 `<script>` 标签提取到 `.js` 文件，HTML 中用 `<script src>` 引用
- 每个弹窗 overlay 同时保留在主页面 DOM 中 + 提取到 `components/` 独立参考文件
- 38 处 `═════` 章节标记注入 HTML 用于导航
- 保留原始备份 `create-order-pad.bak.html`

**零功能回退保证：**
- 所有 DOM ID 保持不变
- 所有 onclick 属性保持不变
- JS 全局变量和事件监听保持不变
- `<script>` 标签位于 `</body>` 前，确保 DOM 元素在先
- 通过 `test_server.js` 可正常访问 `http://localhost:9018/new-order-project/create-order-pad.html`

## create-order-pad.js 死代码分析
对 10,429 行 / 336 个函数声明进行完整的调用图分析：

**分析方法：**
- 扫描所有 `function xxx(` 声明（词频计数 + 调用点追踪）
- 检查 HTML `onclick`/`onchange`/`on*` 属性引用
- 检查 `addEventListener` 注册
- 检查 `.onclick = xxx` 动态赋值
- 检查 `innerHTML`/模板字符串中的 `onclick="xxx()"` 字符串构建
- 跨文件引用检查（所有 `new-order-project/*.html` 和 `*.js`）

**结论：17 个函数确认为死代码**（零引用、零副作用），预估可减少约 120 行代码。

## 死代码清理完成
已从 create-order-pad.js 中删除全部 17 个死函数，文件从 10,429 行缩减至 10,268 行（净减 ~160 行）。
- 删减过程遇到 4 个匹配偏移（navigateTo/getCardConsumeModes/sanitizeWholeOrder* x2），二次定位后全部清除。
- 保留了 `let currentRemarkBtn = null;` 声明（被 alive 代码 openTableRemarkModal / closeRemarkModal / saveRemark 使用）。

## "有限卡" → "有限次卡" 文案替换
在 create-order-pad.js（4处）和 create-order-pad.html（1处）中将所有"有限卡"替换为"有限次卡"：
- JS: 注释、模板字符串、cardTypeText 赋值（2处）
- HTML: add-course-tab-item 标签文本

## customer-detail-pad.html 置换权益弹窗加宽
- 基础宽度：480px → 640px（Pad端）
- PC端（≥1200px）：780px
- 文件：new-order-project/customer-detail-pad.html (L1198-1220)
- 同时调整了 right 的负偏移值以匹配新宽度

## customer-detail-pad.html 分层架构重构
将 5,013 行的单文件拆分为三层架构：
- `customer-detail-pad.html`: 944行纯HTML结构（引用外部CSS/JS）
- `customer-detail-pad.css`: 2,066行独立样式文件
- `customer-detail-pad.js`: 2,212行独立脚本文件

**重构方法：**
- CSS从`<style>`标签提取到`.css`，新增工具类（.d-none/.flex-1/.progress-bar-wrap/.color-danger/.pt-9/.w-360/.w-44/.ws-pre-wrap/p-0）替代内联style
- JS从`<script>`标签提取到`.js`，所有49处静态`onclick`属性转为`addEventListener`绑定
- 动态HTML模板中的onclick保持不变（如toggleBenefitRow/selectCardBenefit等）
- 初始化进度条样式改为JS驱动（替代静态`width:xx%`内联样式）
- 零功能回退：所有DOM ID、全局函数(window.xxx)、事件监听保持不变

## customer-detail-pad.html 权益选择弹窗实现
完成了"置换权益 → 卡内剩余权益 → 选择权益"的权益选择弹窗：
1. **数据补充**：5个 .btn-use 新增 data-spec（60分钟/45分钟/30分钟）和 data-price
2. **弹窗 HTML**：新增 #benefit-select-modal，居中模态弹窗，表格含5列（勾选/权益名称/规格/剩余次数/权益金额）
3. **弹窗 CSS**：640px(Pad)/780px(PC≥1200px)，粉色调选中态，sticky 表头
4. **JS 逻辑**：
   - openExchangeDrawer 存储 cardItem 引用 + 重置卡内权益表格
   - selectCardBenefit 打开权益选择弹窗，从 cardItem 中解析 .btn-use 数据
   - 勾选交互（tr clicked/toggleBenefitRow），已选计数，确认按钮状态联动
   - confirmBenefitSelection 回填到抽屉的"卡内剩余权益"表格（含更换次数步进器+删除按钮）
   - removeCardBenefitRow 删除行，行空时恢复空状态
   - updateExchangeSummary 实时合计已选权益金额

## customer-detail-pad.js Bug 修复
修复重构后运行时错误：
- **根因**：Part 1 事件绑定中 26 处函数引用使用裸名（如 `openMemberMoreInfo`），但函数在 Part 3 中定义为 `window.xxx` 赋值式，未提升
- **修复**：所有裸引用加 `window.` 前缀；`toggleCardDetail` 未定义改为内联实现；`openSupplementModal` 触发按钮补绑
- **额外修复**：置换权益弹窗无数据时移除"选择卡内权益"/"选择置换权益"两个 header 操作按钮，空状态仅展示"暂无权益数据"文本

## 置换权益弹窗 — "更换次数"改为进步器 (Stepper)
- **CSS**：新增 `.exchange-stepper` / `.exchange-stepper-btn` / `.exchange-stepper-value` 等 70+ 行样式。按钮 32×32、圆角 8px、品牌灰白配色，disabled 态 opacity 0.35 + not-allowed，hover/active 有过渡反馈
- **JS Part1**：在 `#exchangeOverlay` 上添加事件委托，处理 stepper 按钮点击 — 增减 ±1、边界校验（min=1 / max=remain）、极值禁用按钮切换、短时品牌色高亮反馈（#E87C9A scale 1.15 → 200ms 恢复）
- **JS Core**：
  - `confirmBenefitSelection`：不再生成 `<input type="number">`，改为生成完整 stepper DOM（减号按钮初始 disabled，加号仅 max≤1 时 disabled）
  - `updateExchangeSummary`：从读 `input.value` 改为读 `.exchange-stepper-value` 的 `textContent`
- 删除按钮交互不受影响，合计金额实时联动进步器值的变化

## 置换权益弹窗 — 恢复 "+ 选择权益" header 按钮
- 卡内权益 和 置换权益 两个 section header 各恢复 `.exchange-btn-secondary` 按钮
- 点击 "选择权益"（置换权益区）→ 触发 `selectExchangeBenefit()`

## 置换权益弹窗 — 按钮拆分为"选择居家产品"+"选择单次护理"
- 置换权益区 header："选择权益"→ 拆为两个并排按钮（`selectHomeProduct()` / `selectSingleCare()`）
- JS: `selectExchangeBenefit` 移除，新增两个独立函数

## 权益选择弹窗确认按钮 Bug 修复
- **根因**：Part 1 IIFE 绑定时 `window.confirmBenefitSelection` 尚未定义（定义在 Part 3），`addEventListener` 收到 undefined
- **修复**：权益选择弹窗（关闭/取消/确认）+ 置换权益抽屉（关闭/取消/确认）共 6 处绑定改为 `function() { window.xxx(); }` 包装

## 置换权益 — 引入"添加居家产品"+"添加单次护理"弹窗
- 从 `create-order-pad` 的 `components/` 和源码复制 HTML/CSS/JS 到 customer-detail-pad：
  - **HTML** (+115行)：`#addServiceModalOverlay`（单次护理，卡片网格+分类筛选）+ `#addProductModalOverlay`（居家产品，表格+左右分类导航）
  - **CSS** (+180行)：添加 CSS 变量 (primary-*/neutral-*/radius-*/shadow-lg) + 全部 add-service / add-product 样式
  - **JS** (+265行)：`selectSingleCare()` / `selectHomeProduct()` 打开对应弹窗；含数据模拟（15护理项+15产品项）、搜索、分类筛选、勾选/移除、清空、确认、localStorage 最近搜索、排序等功能
  - 确认按钮当前 toast 提示 + 关闭弹窗，后续接入回填到置换权益表格

## 置换权益 — 表格回填 + 设为赠送/删除操作 + "赠"徽章
- **HTML**：置换权益 tbody 添加 id `exchangeBenefitTableBody`；表头从 7 列精简为 6 列（权益名称/规格/更换次数/单价/小计/操作）；合计值和备注 span 加 id
- **CSS** (+55行)：`.benefit-item-gift` 赠徽章（复刻 create-order-pad：`#FF4081` 背景、白字、10px、圆角 2px）；`.exchange-action-btn` 操作按钮；`.exchange-qty-input` 次数输入框 focus 态
- **JS** (+80行)：
  - `_exchangeBenefits` 全局数组 + `_renderExchangeTable()` 渲染函数（含 qty input、subtotal 计算、事件委托绑定）
  - `_addToExchangeBenefits(items, type)` 合并新品到列表
  - 两个弹窗确认按钮改为调用 `_addToExchangeBenefits`（type: 'care'/'product'）
  - **设为赠送**：toggle `isGift` → 名称前显示 `<span class="benefit-item-gift">赠</span>`，按钮文字切换"设为赠送"/"取消赠送"
  - **删除**：`_exchangeBenefits.splice(idx, 1)` → 重新渲染
  - 合计金额 `exchangeSummaryValue` 实时联动
