## cltn 项目全量重新部署
- **目标服务器**：lhins-p0csjffm（124.222.211.194，ap-shanghai）
- **部署方式**：Nginx 静态文件服务，root `/var/www/cltn/`，端口 80
- **操作**：rsync 全量同步（含 --delete 清理旧文件），上传后 nginx -s reload
- **访问 URL**：http://124.222.211.194/
- **注意**：Nginx 已配置为纯静态文件服务，无需 Docker

## 修复 upload-files 404 问题 + index.html 重新部署
- **问题**：Nginx `/upload-files/` location 配置 `alias /var/www/upload-files/` 但该目录不存在，文件实际在 `/var/www/cltn/upload-files/`，导致所有 `/upload-files/` 路径 404
- **修复**：移除 Nginx 中 `/upload-files/` 的 location block，由根 root `/var/www/cltn` 统一服务
- **文件名规范化**：`pad&pc-records-mgmt.html` → `pad-pc-records-mgmt.html`（去掉 & 避免 Nginx 路由歧义）
  - 同步更新 `index.html` 和 `pad-pc-upload.html` 中的引用
  - 同步重命名 png：`首页Pad&PC-新增资料管理入口.png` → `首页Pad-PC-新增资料管理入口.png`
